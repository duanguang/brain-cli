# 强制 bin 目录下的脚本文件使用 LF 换行符（Unix 格式）
# 这些文件包含 shebang，必须是 LF 才能在 Unix 系统上执行
bin/** text eol=lf

# Shell 脚本使用 LF
*.sh text eol=lf

# Node.js 脚本在 bin 目录中使用 LF
#!/usr/bin/env node

# 其他文本文件使用默认配置（Windows 上为 CRLF）
* text=auto
